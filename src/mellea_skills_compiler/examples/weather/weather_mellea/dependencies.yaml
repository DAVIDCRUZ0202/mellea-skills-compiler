dependencies:
  - id: dep_004
    category: c6_tools
    disposition: real_impl
    target: tools.py:fetch_weather
    subtype: http
    prerequisites:
      - "HTTP access to wttr.in (public service, no API key required)"
      - "curl binary available on PATH (declared in metadata.openclaw.requires.bins)"
    source_elements:
      - elem_008
      - elem_009
      - elem_010
